#include<stdio.h>
// area of a cicle
int main()
{
	int radius;// does it matter if we use int or float here 
	// i identified radius as a whole number here but when i enter the value of r as 1.55 why did i get the answer.
	printf("enter radius");
	scanf("%d",&radius);
	printf(" area of :%f",3.13* radius * radius);
	return 0;
	}
